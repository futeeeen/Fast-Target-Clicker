# Fast Target Clicker Extension

這個資料夾就是可安裝的 Chrome/Edge 擴充功能本體。

## 安裝方式

1. 打開 Chrome 或 Edge。
2. 進入 `chrome://extensions` 或 `edge://extensions`。
3. 開啟「開發人員模式」。
4. 點「載入未封裝項目」。
5. 選擇這個 `extension` 資料夾。

安裝後，點工具列上的 Fast Target Clicker 圖示，就會在瀏覽器右側開啟工具面板。
